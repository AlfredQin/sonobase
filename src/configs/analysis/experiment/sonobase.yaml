# @package _global_
# Reusable model-only overlay: SonoBase (TriBranchTrunk).
# Pair with `data=<DATASET>` and `prompt_protocol.type=<point|box>` overrides.
defaults:
  - override /model/image_encoder@model.image_encoder: hiera_b_conv_s_conv_t

model_label: sonobase
