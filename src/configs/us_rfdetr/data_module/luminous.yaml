# LUMINOUS lung ultrasound (1 class: b_line).
_target_: nemo_cv.components.datasets.us_rfdetr.SaUSCocoDetDataModule

data_root: ${scratch.dataset_dir}/LUMINOUS
annotation_dir: ${scratch.det_annotation_dir}/LUMINOUS
image_size: ${scratch.image_size}
multiplier: ${scratch.multiplier}
val_from_test: false
