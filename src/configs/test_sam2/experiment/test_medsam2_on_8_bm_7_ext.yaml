# @package _global_
# Sonobase test experiment: TriBranchTrunk evaluated on the 8 benchmark + 7
# external test splits (15 datasets total). This is the real downstream-eval
# target — point `ckpt_path` at the final pretraining checkpoint when ready.

defaults:
  - override /model/image_encoder@model.image_encoder: hiera_t

scratch:
  experiment_name: benchmarks/8_bm_7_ext/medsam2/p0
