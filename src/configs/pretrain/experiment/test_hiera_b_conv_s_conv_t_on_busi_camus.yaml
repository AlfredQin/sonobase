# @package _global_
# Sonobase test experiment: TriBranchTrunk evaluated on the BUSI + CAMUS test
# splits. Used as the smoke target for the test recipe. Pair with the
# busi_camus pretraining checkpoint via `restore_from=` (the run script
# points at experiments/sonobase/pretrain/busi_camus/hiera_b_conv_s_conv_t/LOWEST_VAL).

defaults:
  - override /data: busi_camus_test

scratch:
  experiment_name: sonobase/test/busi_camus_test/hiera_b_conv_s_conv_t
