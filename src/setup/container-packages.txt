# Auto-generated placeholder — NGC PyTorch 24.x approximate baseline.
# Regenerate with: bash src/setup/project-setup.sh --refresh-container-packages
# (runs inside the actual container image configured in container.env)
#
# Format: one package per line, optionally pinned with ==version.
# Lists what the base container already provides, so these packages are not
# installed into the project venv.
torch==2.4.0
torchvision==0.19.0
torchaudio==2.4.0
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.1
pandas==2.2.2
matplotlib==3.9.0
pillow==10.4.0
opencv-python==4.10.0.84
tqdm==4.66.4
transformers==4.44.0
accelerate==0.33.0
datasets==2.20.0
pytest==8.3.2
ipython==8.26.0
jupyter==1.0.0
requests==2.32.3
pyyaml==6.0.1
einops==0.8.0
jsonschema==4.23.0
